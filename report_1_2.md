# [1장 2강 심화] - 딥러닝 적용 판단: 딥러닝 후보 도입을 검토하는 근거 검토
## 실습 배경

루멘 LLM 플랫폼 팀은 사내 문서 질의응답 서비스로 들어오는 문서를 `업무 규정`, `복지`, `보안`, `기타`로 분류하려 합니다. 기획팀은 "딥러닝이면 정확도가 높을 것"이라며 모든 분류를 한 번에 교체하자고 제안했습니다. 그러나 현재 운영 규칙 중에는 문서 제목이 `[보안등급:대외비]`로 시작하면 무조건 보안 문서로 보내는 것처럼 예외가 거의 없는 항목도 있습니다.

반면 자유 형식의 상담 기록은 같은 뜻을 매우 다양한 표현으로 적습니다. 단어 몇 개만 세는 규칙은 `계정이 잠겼다`, `로그인이 막혔다`, `인증에 계속 실패한다`를 서로 다른 사례로 봅니다. 팀은 문제마다 규칙 기반, 사람이 만든 특징을 쓰는 머신러닝 기준선, 표현을 함께 학습하는 딥러닝 중 무엇을 먼저 검토할지 구분해야 합니다.

이번 실습에서는 모델 이름을 고르는 데 그치지 않습니다. 데이터 형태, 규칙 안정성, 라벨 수, 오판 비용을 근거로 후보를 좁히고, 딥러닝을 쓰더라도 검증 데이터에서 단순 기준선과 비교한다는 계약을 만듭니다.

## 실습 목표

- 규칙 기반·머신러닝·딥러닝 접근의 역할을 사례별로 구분한다.
- 원본 텍스트의 표현 다양성이 수작업 특징에 주는 한계를 설명한다.
- 데이터와 운영 제약을 근거로 적용 판단 메모를 작성한다.
- "딥러닝이 가능하다"와 "딥러닝이 우선이다"를 구분한다.

## 진행 방식

- 각 문제는 독립적으로 푼다. 앞 문제의 출력은 다음 문제의 입력이 아니다.
- 주어진 수치는 같은 검증 묶음에서 얻었다고 가정한다.
- 아직 배우지 않은 모델 내부 구조는 판단 근거로 쓰지 않는다.

## 오늘의 업무 흐름

요청 확인 → 데이터와 규칙 점검 → 기준선 선정 → 검증 결과 비교 → 적용/보류 메모

## 상황 자료

```
업무                 데이터          현재 규칙             라벨 데이터
보안등급 라우팅      제목 고정 접두어  변경이 드물고 명확함       400건
상담 주제 분류       자유 형식 문장    키워드 38개             28,000건
신규 제품 불만 탐지  짧은 후기         규칙 없음                 120건
```

## 문제 1. 키워드 규칙의 조용한 실패 찾기

### 업무 요청

상담 주제 분류 규칙은 오류 없이 실행되지만, 문맥을 무시해 잘못 라우팅합니다. 코드와 검증 로그를 보고 실패 유형을 특정하고 최소 두 사례를 설명하세요.

### 수행해야 할 작업

1. 각 문장을 현재 규칙으로 분류한다.
2. 정답과 다른 사례의 인덱스를 찾는다.
3. 단어 존재만 보는 규칙이 놓친 문맥을 설명한다.
4. 고정 규칙을 즉시 폐기할지, 딥러닝 후보와 비교할지 결정한다.

```bash
texts = ["환불이 아니라 교환을 원합니다", "로그인이 막혀 결제가 안 됩니다", "환불 부탁드립니다"]
labels = ["교환", "계정", "환불"]

def current_rule(text):
    if "환불" in text:
        return "환불"
    if "결제" in text:
        return "결제"
    return "기타"
```

### 자주 하는 실수

- `환불`이라는 단어가 있으니 0번이 맞다고 판단한다.
- 오분류 원인을 "데이터가 적다"로만 쓰고 실제 문맥 손실을 지적하지 않는다.


오분류 인덱스:
 1) 환불이 아니라 교환을 원합니다가 단순 룰 베이스 (키워드 규칙)으로 인하여 '환불'label로 분류됨.
 2) '기타' 전 '계정'으로 가는 분류 인덱스가 없음. '로그인 막혀 결제가 안됩니다'가 '기타'로 분류됨.
각 사례의 규칙 출력/정답:
개선 방향: '환불' 검토 전 '교환' 키워드에 따라 '교환'을 먼저 분류하여 개선하고, '기타' 전 '로그인' 등의 '계정' 라벨 분류 규칙을 만들어야함.

```
preds: ['환불', '결제', '환불']
wrong_indices: [0, 1]
```

**해설**
  키워드 일치 여부와 실제 label을 인덱스별로 대조해 실행 성공과 의미 성공을 구분하여야.
  부정·인과 문맥을 잃는 규칙의 오분류만 수집하고 전체 서비스 품질로 과장하지 않아야.
  규칙이 실행되는가보다, 같은 문장 안의 부정 표현과 원인·결과 관계를 보존하는지 확인하여야.

# 단어 존재만 확인하므로 부정과 문장 전체 의미는 보존하지 못합니다.

  먼저 실행 오류와 의미 오류를 분리합니다. 이 코드는 정상 실행되지만 0번에서는 `아니라`를 무시하고, 1번에서는 결제를 결과로 보고 계정 잠김이라는 원인을 놓칩니다. 따라서 자유 문장 분류에는 원문 표현에서 유용한 표현을 학습하는 딥러닝을 후보로 둘 근거가 있습니다.
  
  다만 이 세 문장만으로 교체를 승인할 수는 없고, 충분한 라벨 데이터와 동일한 검증 묶음에서 규칙·단순 기준선과 비교해야 합니다. 규칙에 예외 문구를 계속 추가하는 답은 알려진 세 문장에는 맞지만 새 표현에 일반화된다는 근거가 없습니다.

**접근 순서:** 먼저 규칙의 반환값과 정답을 같은 순서로 나란히 놓고, 불일치한 문장에서 규칙이 실제로 본 단어를 표시합니다. 그다음 부정 표현·원인과 결과·표현 변형 중 어떤 정보가 사라졌는지 사례별로 분리해 기록해야 수정 방향이 구체적입니다.

**오답 원인:** 알려진 문장에 예외 if문을 두 개 추가하는 답은 현재 검증 세 건만 외운 것입니다. 또한 오분류가 두 건이라는 결과만 쓰고 0번의 부정과 1번의 원인 문맥을 구분하지 않으면 딥러닝 후보를 검토하는 근거가 성립하지 않습니다.

**적용 한계:** 이 진단은 키워드 규칙의 문맥 손실을 보여줄 뿐 특정 딥러닝 모델의 우위를 증명하지 않습니다. 실제 교체 판단에는 대표 라벨 데이터, 같은 validation split, 처리 지연과 오류 비용을 고정한 비교 실험이 추가로 필요합니다.

## 문제 2. 적용 판단 도우미 작성하기

### 업무 요청
팀 리드는 반복 회의에서 빠뜨리는 조건을 줄이기 위해 1차 판단 도우미를 요청했습니다. 이 함수는 최종 결재기가 아니라, 어떤 접근부터 검증할지 알려주는 체크리스트 역할입니다.

### 수행해야 할 작업

1. 규칙이 안정적이고 예외가 거의 없으면 `rule_first`를 반환한다.
2. 자유 형식 원본 데이터이며 라벨이 5,000건 이상이면 `deep_learning_candidate`를 반환한다.
3. 그 외에는 `simple_baseline_first`를 반환한다.
4. 세 사례를 모두 실행하고 분기 이유를 설명한다.

검증 가능 정답 코드
샘플 수·규칙 유지비·문맥 의존도를 각각 검사해 딥러닝 적용 여부를 한 조건으로 단정하지 않습니다.
입력 숫자는 팀의 수업용 기준과 비교하고 결과는 배포 승인이 아니라 다음 실험 권고로 제한합니다.

```bash
def recommend_start(raw_unstructured, labeled_count, stable_rule):
    # 명시적이고 안정적인 규칙은 데이터 양과 무관하게 먼저 보존합니다.
    if stable_rule:
        return "rule_first"
    # 원본 텍스트·이미지처럼 표현 설계가 어려우면서 검증 가능한 라벨이 있을 때만 후보로 올립니다.

    if raw_unstructured and labeled_count >= 5000:
        return "deep_learning_candidate"
    # 데이터가 적거나 구조화 입력이면 단순 기준선을 먼저 만들어 비교 기준을 남깁니다.

    return "simple_baseline_first"

cases = [(False, 400, True), (True, 28000, False), (True, 120, False)]
print([recommend_start(*case) for case in cases])
```

출력

```bash
['rule_first', 'deep_learning_candidate', 'simple_baseline_first']
```

## 해설

# raw_unstructured=True는 데이터가 정제되지 않은 "원본 비정형 텍스트(Raw Unstructured Text)" 상태라는 뜻이며, 이는 곧 표현 변형(다양성)이 매우 심하다는 의미.

# 1단계: 보안 접두어처럼 명확하고 확실한 안정 규칙(stable_rule=True)이 있는지 최우선 확인 (규칙이 있으면 데이터가 비정형이든 아니든 무조건 규칙 우선!)

# 2단계: 규칙이 없을 때, 표현 변형이 많은 원본 비정형 데이터인가(raw_unstructured=True) & 학습할 라벨 데이터 수(label_count)가 충분한가를 조합하여 딥러닝 실험 후보인지 판단.

  접근 순서는 stable_rule을 먼저 확인한 뒤 데이터 형태와 라벨 수를 함께 봅니다. 첫 사례는 규칙 자체가 업무 계약이므로 복잡한 모델로 바꿀 이유가 약합니다. 둘째는 표현 변형이 많고 비교 가능한 라벨이 있어 딥러닝 실험 후보입니다. 셋째는 원본 텍스트이지만 120건만으로 바로 복잡한 모델을 승인하지 않습니다. 5,000이라는 숫자는 이 실습의 내부 기준일 뿐 모든 조직에 일반화할 수 없으며, 실제 기준은 데이터 품질·오판 비용·검증 설계와 함께 정해야 합니다.

  접근 순서: 판단 도우미는 가장 확정적인 안정 규칙을 먼저 검사하고, 남은 사례에서 원본 데이터의 비정형성과 라벨 수를 함께 봅니다. 반환값을 낸 뒤에는 어떤 조건이 참이어서 해당 분기로 들어갔는지 사례마다 한 줄씩 남기는 것이 리뷰에 유리합니다.

  오답 원인: 조건 순서를 바꿔 원본 텍스트와 많은 라벨만 먼저 보면 안정적인 보안 접두어 규칙까지 불필요하게 딥러닝 후보가 됩니다. 라벨 수 하나만 기준으로 삼는 답도 데이터의 표현 복잡성과 규칙 안정성을 놓친 것입니다.

  적용 한계: 5,000건은 수업용 내부 기준이며 품질이 낮은 라벨 5,000건이 충분하다는 뜻이 아닙니다. 함수는 실험 시작점을 추천할 뿐 배포를 승인하지 않으며, validation 결과와 운영 제약을 통과해야 다음 결정을 내릴 수 있습니다.

# 주의: raw_unstructured=True만 보고 라벨 수와 상관없이 딥러닝을 고른다(x).


## 문제 3. 불완전한 검증 근거로 승인과 재측정 구분하기

### 업무 요청

상담 주제 분류의 같은 검증 2,000건에서 세 후보를 비교했습니다. 운영팀은 정확도 0.90 이상, 문서당 처리 시간 12ms 이하를 요구하며, latency는 같은 조건에서 최소 3회 반복 측정돼야 승인 자료로 인정합니다. 수치 미달과 증거 부족을 구분해 승인·재측정·제외로 분류하세요.

### 수행해야 할 작업

1. 정확도와 latency 하한을 각각 검사한다.
2. 수치는 통과하지만 반복 측정이 부족한 후보를 `재측정`으로 분리한다.
3. 승인 후보가 없으면 상대적으로 나은 안을 억지로 고르지 않는다.
4. 각 후보의 다음 조치와 검증 범위 한계를 보고한다.

### 상황 로그

후보  접근                  검증 정확도  처리 시간  latency 반복
A     수작업 특징 기준선       0.884       3ms         3회
B     딥러닝 후보             0.921      10ms         1회
C     작은 딥러닝 후보         0.908      13ms         3회

```bash
approved: []
remeasure: ['B']
rejected: {'A': ['accuracy'], 'C': ['latency']}
decision: 보류
```

결과 보고

1. 정확도와 latency 하한을 각각 검사한다.

 가. 정확도 및 latency 하한: 정확도 accuracy 0.90 이상이어야. latency 12ms 이하.
 나. 정확도 미달: 후보 A(0.884)
 다. latency 미달: 후보 C(13ms)

2. 수치는 통과하지만 반복 측정이 부족한 후보를 `재측정`으로 분리한다.

 수치는 통과하지만 반복 측정이 부족한 후보: 후보 B `재측정`(검증 수치 통과, 반복 측정 부족).
# 검증 절차(Evidence): Latency 측정이 동일 조건에서 최소 3회 이상 수행.
# 기준을 충족했으나, Latency 측정 횟수가 3회 미만(1~2회)이거나 측정 횟수 증거가 누락된 경우

3. 승인 후보가 없으면 상대적으로 나은 안을 억지로 고르지 않는다.
  후보 A, 후보 C `제외`

4. 각 후보의 다음 조치와 검증 범위 한계를 보고한다.
  즉, 후보 A `제외`, 후보 C `제외`, 후보 B `재측정`.

# 필수 증거가 모두 있는지 먼저 확인하고 누락된 latency·비용 자료는 임의 값으로 채우지 않습니다.
# 증거가 부족하면 reject가 아니라 remeasure를 반환해 모델 실패와 측정 미완료를 분리합니다.

**해설**

A는 속도는 충분하지만 정확도 미달이고 C는 정확도를 통과하지만 latency를 넘습니다. B는 두 수치만 보면 통과하지만 latency 측정이 한 번뿐이라 승인 자료가 완성되지 않았습니다. 따라서 현재 결정은 `보류`, 다음 조치는 B를 같은 장비·batch 조건에서 두 번 더 측정하는 것입니다. 증거가 부족한 후보를 실패로 단정하지 않되, 좋은 단일 수치를 승인으로 확대하지 않는 것이 핵심입니다.

**운영 티켓에 남길 내용:** 후보마다 위반한 절대 조건과 아직 수집하지 못한 증거를 별도 열에 적습니다. 이렇게 하면 `제외`와 `재측정`이 섞이지 않고, 다음 담당자가 무엇을 확인하면 보류를 해제할 수 있는지도 바로 알 수 있습니다.

**왜 B를 바로 고르면 안 되는가:** 반복 수가 1이라는 사실을 무시하면 일시적인 장비 부하나 warm-up 효과를 정상 latency로 오인할 수 있습니다. 반대로 B를 완전 탈락시키면 수치 조건을 이미 통과한 후보의 보완 가능한 증거 부족을 영구 실패로 잘못 처리합니다.

검증 2,000건도 실제 유입 문서의 표현과 비율을 완전히 대표하지 않을 수 있습니다. B의 반복 측정을 통과해도 결과는 제한된 1차 승인 자료이며, 희귀 안전 사례와 실제 부하 분포를 포함한 후속 검증이 남습니다.


## [1장 3강 심화] - 학습 파이프라인 순서 이해 실습 (1) ##
 
## 실습 배경

루멘 팀의 문서 민감도 분류기는 야간 학습 뒤 검증 손실이 갑자기 좋아졌습니다. 그런데 코드를 확인하니 검증 구간에서도 파라미터가 바뀐 흔적이 있습니다. 로그 숫자는 좋아 보이지만, 평가할 데이터로 모델을 다시 학습했다면 그 수치를 신뢰할 수 없습니다.

주니어 엔지니어는 전체 수학을 아직 배우지 않았어도 데이터 → 모델 출력 → 손실 → gradient 초기화 → 역전파 → 업데이트 → 검증이라는 큰 흐름과 단계별 산출물을 구분해야 합니다. 특히 `model.eval()`은 평가 모드를 정하고, `torch.no_grad()`는 gradient 추적을 끄며, 둘 다 파라미터 업데이트 명령은 아니라는 점을 계약으로 남깁니다.

이번 페이지에서는 긴 학습 코드를 외우지 않고, 단계의 의존 관계를 근거로 순서 오류와 검증 오염을 찾아냅니다.

## 실습 목표

- 한 batch 학습 단계의 선후 관계를 검증한다.
- 데이터·logits·loss·gradient·업데이트의 산출물을 구분한다.
- 학습과 검증에서 허용되는 동작을 분리한다.
- 로그가 좋아도 평가 계약이 깨지면 배포를 보류할 수 있다.

## 진행 방식

- `zero_grad → forward → loss → backward → step`을 학습 계약으로 사용한다.
- 검증에서는 `step`을 호출하지 않는다.
- 실제 값보다 단계 순서와 파라미터 변경 여부를 우선 확인한다.

## 오늘의 업무 흐름

실행 기록 수집 → 단계 의존성 검사 → 한 batch 코드 복구 → 검증 오염 판정

## 상황 자료

02:10 validation start
02:10 valid_loss=0.412
02:10 optimizer.step called
02:10 validation end


## 문제 1. 뒤섞인 실행 기록 진단하기

### 업무 요청

장애 재현기가 남긴 단계 목록에서 첫 번째 계약 위반을 찾고 올바른 순서를 출력하세요.

### 수행해야 할 작업

1. `backward`는 `loss` 뒤인지 확인한다.
2. `step`은 `backward` 뒤인지 확인한다.
3. `zero_grad`가 새 gradient 계산 전에 있는지 확인한다.
4. 첫 위반과 복구 순서를 함께 보고한다.

observed = ["forward", "loss", "step", "zero_grad", "backward"]

```bash
  first_violation: backward must precede step
  recommended: zero_grad -> forward -> loss -> backward -> step
```

PyTorch 학습 루프(Training Loop)에서 반드시 지켜야 하는 '호출 순서 의존성'이 제대로 지켜졌는지 검사하는 코드입니다.
주어진 코드의 observed는 잘못된 순서로 작성되어 있어서, 이 코드를 실행하면 순서 위반 에러 메시지를 출력합니다.

- 절대로 어기면 안 되는 의존 관계 (dependencies) PyTorch 연산의 논리적 순서 규칙입니다.
("forward", "loss") : forward 계산이 먼저 나와야 loss를 계산할 수 있음 (위치 index: 0 < 1 ⭕️)
("loss", "backward") : loss 계산이 끝나야 역전파(backward)를 할 수 있음 (위치 index: 1 < 4 ⭕️)
("zero_grad", "backward") : 기존 기울기를 초기화(zero_grad)한 뒤 역전파를 해야 함 (위치 index: 3 < 4 ⭕️)
("backward", "step") : 역전파(backward)로 기울기를 계산한 뒤 가중치를 업데이트(step)해야 함 (위치 index: 4 < 2 ❌ 위반!)

## 해설

  위치 검사는 단지 문자열 정렬 문제가 아니라 산출물 의존성을 확인합니다. 관찰 목록의 치명적 위반은 `step`이 새 gradient를 만드는 `backward`보다 먼저 실행된 것입니다. `zero_grad`는 이전 gradient를 새 `backward` 전에 비워야 하지만, `forward`와 `loss`는 gradient buffer를 소비하지 않으므로 그보다 먼저 실행돼도 기술적으로 유효합니다. 다만 한 step의 경계를 읽기 쉽도록 보통 시작 부분에 두는 순서를 권장합니다.

**리뷰에서 먼저 볼 의존성:** forward가 출력을 만들고 loss가 그 출력을 소비하며, backward가 gradient를 만든 뒤 step이 이를 소비합니다. zero_grad의 필수 조건은 forward 이전이 아니라 새 backward 이전입니다. 이 네 edge를 따로 적으면 관례적인 배치와 실제 오류 조건을 구분할 수 있습니다.

**오답 원인:** 다섯 호출이 모두 한 번씩 등장한다는 사실만 확인하면 step이 gradient보다 먼저 실행되는 오류를 놓칩니다. 관찰 목록을 알파벳순으로 바꾸거나 단순히 정답 목록을 외워 쓰는 것도 산출물 의존성을 설명하지 못합니다.

**누적 학습에서는:** gradient 누적을 의도하면 매 batch마다 zero_grad하지 않을 수 있습니다. 그 경우에도 누적 구간 시작 전에 비우고 마지막 backward 뒤에 한 번 step한다는 별도 계약이 필요합니다. 따라서 권장 한 줄 순서를 유일한 유효 순서로 일반화해서는 안 됩니다.


## 문제 2. 한 batch 학습 코드를 직접 복구하기

### 업무 요청

문서 임베딩을 점수 하나로 바꾸는 작은 회귀 모델입니다. 한 batch에서 실제로 파라미터가 바뀌도록 코드를 완성하고, loss 전후가 아니라 weight 전후를 검증하세요.

### 수행해야 할 작업

1. seed를 고정하고 모델·loss·optimizer를 만든다.
2. 학습 계약 순서대로 한 step을 실행한다.
3. 업데이트 전후 weight를 복사해 비교한다.
4. loss와 변경 여부를 출력한다.

```bash
print(f"Weight 업데이트 변화량: {weight_diff:.6f}")
assert weight_diff > 0, "Weight가 업데이트되지 않았습니다!"
print("✅ 한 batch 학습 후 Weight가 성공적으로 변경되었습니다.")
```
업데이트 전 Tensor는 detach().clone()으로 별도 보관해야 나중 값과 비교할 수 있습니다.


```
loss_is_scalar: True
weight_changed: True
```

**해설**

접근 순서는 단계 이름을 코드로 옮긴 뒤, 실제 계약이 지켜졌는지를 파라미터 복사본으로 확인하는 것입니다. `loss.item()`만 출력해도 코드는 돌아가지만 업데이트가 일어났다는 증거는 아닙니다. `before = model.weight`로만 저장하면 같은 파라미터 객체를 계속 바라보므로 전후 비교가 무너집니다. 이 예제는 한 batch의 흐름만 검증하며, loss가 장기적으로 감소하거나 일반화 성능이 좋아진다는 보장은 하지 않습니다.

**접근 순서:** 모델과 optimizer를 만든 직후 update 전 weight를 분리 복사하고, 계약 순서대로 한 step을 실행합니다. 마지막에는 loss scalar 여부와 weight 변경을 별도로 검사해 계산 성공과 update 성공을 서로 다른 증거로 남깁니다.

**오답 원인:** loss가 출력됐다는 사실만으로는 backward나 step이 호출됐는지 알 수 없습니다. 또한 before에 parameter 객체 자체를 저장하면 after와 같은 저장공간을 보게 되어 변경량 검사가 무의미해지므로 detach와 clone이 필요합니다.

**적용 한계:** 한 번의 weight 변경은 코드 경로가 연결됐다는 뜻이지 좋은 방향으로 충분히 학습됐다는 보장은 아닙니다. 여러 step의 loss 흐름과 validation 결과는 후속 실험에서 확인해야 하며, 이 예제의 회귀 데이터 두 건을 운영 성능으로 일반화할 수 없습니다.